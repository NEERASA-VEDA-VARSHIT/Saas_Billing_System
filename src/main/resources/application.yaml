spring:
  config:
    import: optional:file:./application-local.yaml
  application:
    name: saas-billing-system
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: saas-billing-group
  mail:
    host: smtp.gmail.com
    port: 587
    username: 2024eb02265@online.bits-pilani.ac.in
    password: kpaemnijrjbyzvgw
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

springdoc:
  api-docs:
    path: /v3/api-docs
  packages-to-scan: com.project.saas_billing_system.controller
  paths-to-match: /api/**
  show-actuator: false

app:
  upload:
    dir: ./uploads
    max-file-size: 10MB
  mail:
    from: 2024eb02265@online.bits-pilani.ac.in
  jwt:
    secret: ${JWT_SECRET:defaultSecretKeyForJwtTokenGenerationThatIsAtLeast256BitsLong}
    expiration-ms: 86400000
  rate-limit:
    enabled: true
    max-requests-per-window: 100
    window-seconds: 60

---
spring:
  config:
    activate:
      on-profile: default
  datasource:
    url: jdbc:h2:mem:saas_billing;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true

---
spring:
  config:
    activate:
      on-profile: postgres
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/saas_billing?sslmode=require}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
